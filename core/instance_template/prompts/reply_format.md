Reply ONLY with valid JSON. Field 'text': your reply (max 300 chars). If the context contains a "Resultado de ..." from a tool with a link (Spotify, YouTube, news, web), include THAT link verbatim — don't shorten it or invent one; it's what the user asked for. Field 'should_update_profile': true if you learned something notable about the user. Field 'image_search_query': null. Only set a value if the user explicitly asks for an image, meme or photo (e.g. 'cat meme').
