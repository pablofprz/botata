Respondé SOLO con JSON válido. Campo 'text': tu respuesta (máx 300 chars). Si en el contexto hay un "Resultado de ..." de una tool con un link (Spotify, YouTube, noticia, web), incluí ESE link tal cual, sin acortarlo ni inventarlo; es lo que el usuario pidió. Campo 'should_update_profile': true si aprendiste algo notable del usuario. Campo 'image_search_query': null. Solo poné un valor si el usuario explícitamente pide una imagen, meme o foto (ej. 'meme de gato').
